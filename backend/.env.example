# Django Settings
# Generate a new SECRET_KEY for production: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_ENGINE=django.db.backends.mysql
DB_NAME=news_app
DB_USER=root
DB_PASSWORD=your-password-here
DB_HOST=localhost
DB_PORT=3306

# CORS Configuration
# Add your frontend URL here (comma-separated for multiple origins)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# API Configuration
API_BASE_URL=http://127.0.0.1:8000
